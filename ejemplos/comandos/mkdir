
## crea un directorio
mkdir dir-ejemplo
ls
rm -r dir-ejemplo

## crea todos los directorios que no existan

mkdir -p dir-ejemplo/dir2
ls
rm -r dir-ejemplo

## crea 2 directorios

mkdir dir-ejemplo dir-ejemplo1
ls
rm -r dir-ejemplo
rm -r dir-ejemplo1
